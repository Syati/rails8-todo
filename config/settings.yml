database:
  url: # URL is intentionally unset; use discrete connection settings instead.
  rails_max_threads: 5
  name: rails8_todo
  username: rails8_todo
  password: <%= Rails.application.credentials.dig(:database, :password) %>
  host: # override on each env
  port: # override on each env
