commit push

---

Error: Attempted to load application default credentials since neither `credentials` nor `access_token` was set in the provider block. No credentials loaded. To use your gcloud credentials, run 'gcloud auth application-default login'. Original error: google: could not find default credentials. See https://cloud.google.com/docs/authentication/external/set-up-adc for more information

---

これって gui　でつくるってことだよね？

---

bootstrap って dir 作りますか？

---

そうしましょう

---

bootstrap で構築した後はどうやってメンテするの？

---

それもしてくれる？

---

WIFってなに？

---

terraform init -bakend=false はできたけど apply すると│ Error: HCP Terraform or Terraform Enterprise initialization required: please run "terraform init"
│
│ Reason: Initial configuration of HCP Terraform or Terraform Enterprise.
│
│ Changes to the HCP Terraform configuration block require reinitialization, to discover any changes to the available workspaces.
│
│ To re-initialize, run:
│   terraform init
│
│ Terraform has not yet made changes to your existing configuration or state.